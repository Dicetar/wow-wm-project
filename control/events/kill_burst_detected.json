{
  "id": "kill_burst_detected",
  "schema_version": "control.event.v1",
  "enabled": true,
  "event_type": "kill_burst_detected",
  "description": "The deterministic rule engine observed enough kills inside the configured time window.",
  "required_fields": ["event_id", "player_guid", "subject_type", "subject_entry"],
  "preferred_source": "wm.rules"
}
